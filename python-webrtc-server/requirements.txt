# Nova S2S WebRTC Python Requirements
# Install after creating conda environment with: pip install -r requirements.txt

# Core packages with tested versions
# numpy installed via conda as av dependency
scipy==1.11.4
opencv-python==4.11.0.86

# WebRTC and media processing
# av=11.0.0 installed via conda for proper FFmpeg linking
aiortc==1.6.0

# ML/AI packages
ultralytics==8.3.0

# AWS SDK and Bedrock (requires Python 3.12+)
boto3>=1.34.0
botocore>=1.34.0
aws-sdk-bedrock-runtime>=0.0.1
# Ensure all smithy components are compatible
smithy-aws-core>=0.1.0
smithy-core>=0.1.0
smithy-http>=0.2.0

# Web and networking
websockets>=11.0
aiohttp>=3.8.0
requests>=2.28.0

# Configuration and utilities
python-dotenv>=1.0.0
psutil>=5.9.0
pillow

# Agent and MCP integration
mcp>=1.0.0
strands-agents>=0.1.0

# Voice Activity Detection
webrtcvad>=2.0.10